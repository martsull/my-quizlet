# URL of the deployed Cloudflare Worker
# In dev, leave unset — Vite proxy forwards /api → localhost:3000
VITE_API_URL=https://vocab-api.your-subdomain.workers.dev

# Base path for GitHub Pages (set to /your-repo-name/)
# In dev, leave unset (defaults to /)
VITE_BASE_PATH=/my-quizlet/
